# 特攻表(maintable)の生成

`maintable` の全セルは手入力せず、イベントごとのルール定義から生成する。
2026夏イベの場合、342行 × 10列 = 3,420セルの実体は、出典に書かれている40個ほどの数値と艦名リストだけ。

## 構成

| ファイル | 役割 |
|---|---|
| `data/events/event<N>.rules.mjs` | 倍率ルール。**ここだけ直せば表全体が直る** |
| `data/ship-meta.mjs` | 国籍(形態単位)と特攻グループ分割の定義 |
| `scripts/fetch-master.mjs` | Firestore のマスタを `data/master/` にダンプ(読み取りのみ) |
| `scripts/build-maintable.mjs` | 生成器。`out/` に CSV・投入用JSON・検証レポートを出す |

`out/` と `data/master/` は生成物なので git 管理外。

対地装備(`shiplist.ground_atk`)の生成については後段の「対地装備の生成」を参照。

## 計算モデル

```
セルの値 = (全図の艦種倍率) × (全図のグループ倍率) × (ボス地点の艦種倍率) × (ボス地点のグループ倍率)
```

`maintable` の列(`mapId_N`)は `eventmap` のボス地点に対応する。道中・ギミック地点は列が無いので載せない。
装備由来の特攻(瑞雲系の機数倍率、特四式内火艇など)は艦単位で表現できないため対象外。

## 倍率を修正するとき

```sh
node scripts/fetch-master.mjs          # マスタを最新化
# data/events/event3.rules.mjs の数値を直す
node scripts/build-maintable.mjs 3
```

`out/report.md` の**警告が0件**であることを必ず確認する。警告が出るのは主に次の場合。

- 艦名が shiplist で解決できない
- グループ内に艦種の違う形態が混在し、倍率が割れる(→ `ship-meta.mjs` の `SP_GROUP_SPLITS` で分割)
- 外国艦の国籍が推定できない

Firestore への反映は `D:\src\firebase-maintenance` 経由。**既存行の更新なので、必ず sync してから貼る**
(シートの行に Firestore のドキュメントIDが入っていないと、更新ではなく新規作成になり重複する)。

```sh
cd D:\src\firebase-maintenance
node index.js sync   --collections maintable   # Firestore → シート(ID付きの現在値)
# シートの該当イベント行に out/maintable_event3.csv の値を貼る(orig 列で突き合わせること)
node index.js export --collections maintable   # シート → Firestore
```

## 後段作戦を追加するとき

1. `eventmap` に後段のボス地点を追加する(mapId は既存の続き番号)。
2. `fetch-master.mjs` を実行して eventmap を取り込む。
3. ルール定義に stage を追加する。`nodes[].mapIds` は eventmap の mapId と一致させる。
4. `build-maintable.mjs` を実行。前段の列も同じ値で再生成されるので、表全体をそのまま貼り替えればよい。
5. 上記の sync → 貼り付け → export。

## 検算について

ルール定義の `checks` に、外部の特効表(X投稿の画像など)から読み取った期待値を書いておくと、
生成結果と突き合わせて `out/report.md` に差分が出る。ソース同士の食い違いはここで炙り出す。
**出典(Googleドキュメント)を正とし、画像は検算にのみ使う**方針。

---

# 対地装備(ground_atk)の生成

`shiplist.ground_atk` は**形態(bannerId)単位**の対地装備の可否。艦単位ではないので手入力しない。

| 値 | 意味 | SPA の「対地装備」列 |
|---|---|---|
| 0 | どちらもNG(改装しても解禁されない) | `-` |
| 1 | 上陸用舟艇のみ可 | `大発系のみ` |
| 2 | 特型内火艇のみ可 | `内火艇のみ` |
| 3 | どちらも可 | `大発系・内火艇OK` |
| 4 | 今は不可。改装すると上陸用舟艇が解禁 | `(改造後)大発系のみ` |
| 5 | 今は不可。改装すると特型内火艇が解禁 | `(改造後)内火艇のみ` |
| 6 | 今は不可。改装すると両方解禁 | `(改造後)大発系・内火艇OK` |

1〜3 が「今の形態で装備できる」、4〜6 が「改装を進めれば装備できる」。
4〜6 は `3 + (改装後に解禁されるもの)` なので、3を引けば何が解禁されるか分かる。
対地装備は改装で解禁される艦が大半のため、単に 0 と切り捨てず区別している。

出典: <https://kannagi35.com/content/kantai-collection-anti-ground>
(出典の WG系・迫撃砲系は本アプリの対象外)

## 構成

| ファイル | 役割 |
|---|---|
| `data/ground-atk.rules.mjs` | 出典の表の転記。**ここだけ直せば全形態の値が直る** |
| `scripts/build-ground-atk.mjs` | 生成器。`out/` に CSV・投入用JSON・レポートを出す |

```sh
node scripts/fetch-master.mjs      # マスタを最新化
node scripts/build-ground-atk.mjs
```

`out/ground_atk_report.md` の**警告が0件**であることを必ず確認する。

## 生成の考え方

出典は「艦名」で書かれているが、可否は改装段階で変わる(秋津洲は素で不可・改で可)。
そのため生成器は次の順で形態(bannerId)に落とす。先に決まった規則が優先される。

1. **出典の表** — `SOURCE_ROWS` の転記。`[上陸用舟艇, 特型内火艇]` で書く。
   「できない」行(`[0,0]`)も残す。確認済みの記録であり、下の継承を止める役割も持つ。
2. **出典の抜けの補完** — `SOURCE_FIXES`。出典に行が無いが装備可能と確認できた形態。
   **出典を直接転記していない唯一の箇所**なのでレポートで必ず確認する。
3. **改装形態への継承** — 出典は「千歳」「あきつ丸」のように基本形しか載せない艦がある。
   同一系統・**同一艦種**で updateLevel が真に大きい形態に引き継ぐ。
   艦種が変わる形態(千歳航=軽空母)は装備可否も変わるため引き継がない。
   分岐改装で可否が割れる形態が出たら `INHERIT_BLOCK` で止める。
4. **艦種一括ルール** — `TYPE_RULES`。出典の本文にあり表に行が無いもの
   (「まるゆを除く全ての潜水艦・潜水空母は特型内火艇を装備できる」)。
5. 残りは 0。
6. **改装後に解禁(4〜6)** — ここまでで 0 になった形態について、改装先に装備できる形態があれば
   `3 + (解禁されるもの)` にする。ルール定義には書かない(改装系統から自動で導出する)。

継承した形態・補完した形態・改装後に解禁される形態はレポートに全件出るので、そこだけ目視すればよい。

改装系統を辿るキーは **`orig`(系統ID)であって `spGroupId` ではない**。`spGroupId` は特攻用の分割で
改装経路をまたぐことがあるため(三隈 → 三隈改二特、神威 → 神威改 が別グループになっている)。

分岐改装で解禁されるものが食い違う場合(「大発だけ解禁する改装」と「内火艇だけ解禁する別の改装」に
分かれ、両立する形態が無い)は OR で束ねると誤読されるため、レポートに警告として出す。

## Firestore への反映

既存行への列追加なので、**必ず sync してから貼る**(docId が無いと新規作成になり重複する)。

```sh
cd D:\src\firebase-maintenance
node index.js sync   --collections shiplist   # Firestore → シート(docId付きの現在値)
# シートに ground_atk 列を作り、out/shiplist_ground_atk.csv の値を docId で突き合わせて貼る
node index.js export --collections shiplist   # シート → Firestore
```

## 新しい艦・改装が実装されたとき

shiplist に形態が増えたら `fetch-master.mjs` → `build-ground-atk.mjs` を回す。
出典に載っているのに shiplist に無い艦名は「【艦名未解決】」として警告に出るので、
先に shiplist 側へ形態を追加すること。

---

# 装備特攻(eqattack / eqrate)の生成

装備由来の特攻は艦単位の `maintable` では表現できないため、別テーブルに持つ。
どちらも `eventId` 列を持つ**単一コレクション**で、イベントが増えても行が増えるだけ。

| コレクション | 単位 | 役割 |
|---|---|---|
| `eqattack` | 装備1件 = 1行 | 所属グループ(`grp1`〜`grp3`)。装備種別・ステータスは公式マスタから引いた値を持たせる |
| `eqrate` | グループ × 個数 = 1行 | 地点ごとの倍率。`maintable` と同じ横持ちで列は `mapId_N` |

## 計算モデル

特攻の掛かり方は2種類あり、`eqrate` の `count`(積んだ数)列で両方を表す。

| 型 | 例 | `eqrate` の行 |
|---|---|---|
| 同組は重複しない | 後段(E-4/E-5)の A/B/C 組、対地の GA/GB/GC | `count=1` の1行だけ |
| 積んだ数で倍率が変わる | 前段(E-3)の飛機A | `count=1,2,3…` の複数行 |

クライアントの計算規則は1つで足りる。

> 組ごとに、選択中の装備のうちその組に属する数を `n` とする。
> **`count ≤ n` かつそのマスに値がある行のうち、`count` が最大のもの**の倍率を掛ける。

A/B/C 組は `count=1` の行しか無いので、何個積んでも1回しか掛からない(出典ルール5)。
飛機A は E-3-1(O) が `1→1.08 / 2→1.16`、E-3-4(Z) が `1→1.11 / 2→1.18 / 3→1.28` と
マスごとに表が違い、増分も一定ではない。O で3個積んだ場合は `count=3` の行に O 列の値が無いため
`count=2` の 1.16 が使われ、出典の「≥2 1.16x」と一致する。

艦上の A 組だけは全マップ倍率を持ち、個別マス倍率と積算する。
```
艦上 = A組の全マップ倍率 × A組の個別マス倍率 × B組の個別マス倍率 × 機数倍率の組
基地 = C組の個別マス倍率
```
艦上(空母用・空母以外)は **A・B 組のみ**、基地は **C 組のみ**有効。
`eqrate` にはこの積を畳んだ値が入るので、クライアントは所属グループの `mapId_N` を掛けるだけでよい。

対地装備は各装備が1組だけに所属するため、同じ表に `GA`/`GB`/`GC` 組として載せている。

## 構成

| ファイル | 役割 |
|---|---|
| `data/events/event<N>.equipment.mjs` | 倍率と所属グループ。**ここだけ直せば両テーブルが直る** |
| `scripts/fetch-slotitem.mjs` | 装備マスタ(艦これの `api_start2`)を `data/master/` にダンプ |
| `scripts/build-eqtable.mjs` | 生成器。`out/` に CSV・投入用JSON・レポートを出す |

```sh
node scripts/fetch-master.mjs      # eventmap を最新化
node scripts/fetch-slotitem.mjs    # 装備マスタを最新化
node scripts/build-eqtable.mjs 3   # eventId
```

`out/eq_report.md` の警告を必ず確認する。Firestore への反映は maintable と同じ手順。

## 装備の一意キーと種別

装備名・`api_slotitem_id`・装備種別・ステータスは**艦これ公式マスタが正**で、ルール定義には書かない。
ルール定義に書いた装備名が公式マスタで引けなければ生成は**エラーで止まる**(黙って落とさない)。
艦隊分析コードの装備データも同じ `api_slotitem_id` なので、そのまま突き合わせられる。

`api_start2` は公開APIから直接は取れないため、応答そのままのダンプを入力にしている。
既定では kc-web(<https://github.com/noro6/kc-web>) が同梱している `public/START2.json` を読む。
別のダンプを使う場合は `node scripts/fetch-slotitem.mjs <パス>`。

表示用の種別は公式の装備種別から機械的に決める(名前による例外指定はしない)。

- 艦上(`eqType`) — 艦戦 / 艦攻 / 艦爆 / 艦偵 / 水戦 / 水偵 / 水爆。基地専用の装備は空。
- 基地(`baseType`) — 陸上/水上 × 攻撃機/戦闘機/偵察機 の6分類に畳む。
  艦上機も基地から飛ばすので陸上側に入る(艦戦→陸戦、艦攻・艦爆→陸攻、艦偵→陸偵)。
  大型陸上機・噴式戦闘爆撃機は陸攻、大型飛行艇は陸偵に寄せている。

## 対象範囲

装備特攻があるのは **E-3(飛機A) と E-4/E-5(A/B/C組・対地)** だけ。E-1/E-2 は出典に記述が無い。

出典には載っているが、この表に入れていないものが2つある。

- **道中マスの倍率**。飛機A は E-3 の道中(G/I/K/L/N 等)にも倍率があるが、`eventmap` が
  ボス地点しか持たないため列が無い。maintable と同じ方針。
- **艦種が条件の装備特攻**。E-3-3(X) の「AV装備 特四式内火艇/改 1.45x」「SS装備 特四式内火艇/改 1.95x」
  「SS装備 晴嵐系列+零式小型水上機 1.25x」(いずれも不可叠加)。装備だけでは決まらず艦種に依存するため、
  艦種非依存のこの表では表現できない。対応するなら `eqrate` に適用艦種の列を足すことになる。

なお飛機A と晴嵐系列には「打空無効」(搭載数0のスロットに置くと無効)の注記がある。倍率計算には
影響しないため表には持たせていない。

## 出典の未解決点

- **C1組の倍率が出典全域で不明**。所属装備(深山改/深山)はあるので組自体は残し、値は空のまま。
- **E5 の B2/B3 に「全マップ有効」値(1.07/1.12)が付いた**が、個別マス倍率と積算するのか、
  個別マス値が既に総倍率なのかが読み取れない。現状は後者を採用し、`unresolvedMapWide` に
  数値を残したうえでレポートに注意書きを出している。前者なら E-5-2(J2) の B2 は 1.07×1.1148 になる。
